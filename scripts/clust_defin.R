# Defining cluster names and colours
# John Godlee (johngodlee@gmail.com)
# 2019_12_12

##' Mopane, Baikiaea, core Miombo and marginal Miombo 
##' not sure about latter two, but one seems to me to be 
##' degraded/disturbed miombo, other not, should get co-authors opinion

# Define a 4 point colour palette for vegetation clusters
clust_pal <- c("#5cb930", "#6e49cc", "#49b9da", "#da2c57")

# barplot(height = rep(1, length(clust_pal)), width = rep(1, length(clust_pal)), 
#   col = clust_pal, axes = FALSE)

clust_names <- c("Sparse miombo / Baikiaea", "Core miombo", "ex-Acacia", "Mopane")

